# 精度对齐

要对齐两个不同runtime的模型，需要比较统一输入产生的输出结果。

但是衡量指标却是一个问题。目前已知的就是相对误差和绝对误差，但感觉这两个在deep learning里面都不太好用。

## 绝对误差

没有考虑数据本身的规模。

## 相对误差

容易受到很小值的影响，比如-1e-8和1e-8的相对误差是100%。

## 改进

我觉得需要考虑数据本身的norm。

先调研一下pytorch的实现：isclose和allclose。

